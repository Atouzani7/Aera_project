import StatusLayout from "./ui/StatusLayout";

export default function Loading() {
    return (
        <StatusLayout
            title="Chargement en cours..."
            message="Un petit instant ⏳"
        />
    );
}
